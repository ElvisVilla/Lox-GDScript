@tool
extends Node

class GDBrances  {

	// new way of  declaration
	var version: 0.1 // Implicit float
	var updateButton: Button() // declared and initialized  
	var removeButton: Button // just declared

	var isReady := false // Inferred type
	var name : String = "GDBraces" // Type hint

	func getName() String {
		return name
	}

	func setName(name: String) {
		self.name = name
	}

	// optional parameter
	func setVersion(version: 0.1) { 
		self.version = version
	}
}
