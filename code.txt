class Weapon : Area2D {

	var damage: int = 70
	var rangeUnits: float = 150.0
	var shouldStop: bool = false

	var calculateDamage : float = 200

	func init(_damage, _rangeUnits, _shouldStop) {
		self.damage = _damage
		self.rangeUnits = _rangeUnits
		self.shouldStop = _shouldStop
	}


	func attack() {
		if damage < 100 or rangeUnits > 1 {
			if damage < 100 or rangeUnits > 1 {
				shouldStop = false
			}
		}
		damage = 200
		damage = 200
	}

	func stopAttackAnimation() {
		shouldStop = true
	}
}
